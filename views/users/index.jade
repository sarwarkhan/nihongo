extends ../layout

block content
  if (!user)
    a.btn.btn-primary.btn-block(href="/users/login") Login
    a.btn.btn-warning.btn-block(href="/users/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a.btn.btn-danger.btn-block(href="/users/logout") Logout
